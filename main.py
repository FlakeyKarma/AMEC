import resources as F

if __name__ == '__main__':
    #Initialize Object
    fn = F.FUNCTIONS()
    #Start
    fn.start()
    #Make the names for the object
    fn.NameMake()
    #Run the commands
    fn.CMD()
    #Completion
    fn.finish()
